/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['*'], // Allows all domains (not recommended for production)
    },
  };
  
  module.exports = nextConfig;
  